<h1>Follow Users</h1>
<% @user.followings.each do |user| %>
<table class='table'>
	<thead>
	<tr>
		<th>image</th>
		<th>name</th>
	</tr>
</thead>
<tbody>
	<tr>
		<td><%= attachment_image_tag(user, :profile_image, :fill, 50, 50, fallback: "no-image-mini.jpg") %></td>
		<td><%= user.name%></td>
		<td><%= render 'relationships/stats_2' ,user: user　%></td>
		<td>
			<%= render "relationships/follow_form_2",user: user %>
		</td>
		<td>
			<%= link_to "show", user_path(user) %>
		</td>
	</tr>
</tbody>
</table>
<% end %>
